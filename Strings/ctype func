
#include <stdio.h>
#include <string.h>
#include <ctype.h>
int main()
{
    char str[10];int count=0;
    scanf("%s",str);
    for(int i=0;i<strlen(str);i++){
        if(islower(str[i]))
        str[i]=toupper(str[i]);
    }
    printf("%s",str);
    return 0;
}
